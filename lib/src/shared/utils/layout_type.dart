/// Represents the layout type.
///
/// The `LayoutType` abstract class serves as the base class for different layout types in the application.
/// It provides a common interface for defining and working with different layout types.
///  `FlexiLayout` uses a parameter called `layout` to specify the layout type.
///  `LayoutType` is a wrapper class for the different layout widgets 
// ignore_for_file: lines_longer_than_80_chars

abstract class LayoutType {}
